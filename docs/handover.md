# Где мы остановились

> Заметка на возобновление работы. Обновлена 30 июля 2026.

## Состояние

Ветка `claude/project-concept-review-3382m5` и `main` стоят на одном
коммите. Проверки зелёные: 348 юнит-тестов, чистые линтер, тайпчек и
сборка. Сайт работает на jivoetelo.ru, бот подключён, секреты сервера
перевыпущены.

**В `main` въехали две миграции, которых на сервере ещё нет** —
`0009_profile-pace` и `0010_profile-target-weight`. Они добавляют колонки в
`profiles` и применятся сами: `deploy/migrate.sh` вызывается выкаткой и
ведёт учёт в `schema_migrations`. Ручных действий не нужно, но выкатку
после них стоит проверить.

Что ещё не сделано и почему:

- **Автодеплой не работает** — в репозитории не заданы `DEPLOY_HOST`,
  `DEPLOY_USER`, `DEPLOY_SSH_KEY`, `DEPLOY_KNOWN_HOSTS`. Пуш в `main`
  запускает workflow, и тот честно падает с понятным сообщением. Пока
  выкатка ручная: `git pull && docker compose up -d --build`.
- **Бэкапы не в кроне.** `deploy/backup.sh` готов и проверен, строка для
  crontab — в `deploy-vps.md`, раздел про бэкапы.
- **Разбор включён.** Уведомление РКН подано, ИП зарегистрировано
  (30 июля 2026). `AI_PROVIDER` пуст, работают боевые ключи через прокси.
  Выключить снова — `AI_PROVIDER=off`; `mock` в продакшене означает то же
  самое, выдуманных цифр он не покажет (`lib/ai/mode.ts`).

## Сделано в последней волне: инбокс, напоминания, почта

1. **Telegram-бот с фото-инбоксом.** Апдейты приходят вебхуком в
   `/api/tg/webhook` — отдельного процесса нет, на VPS это экономит целый
   контейнер. Снимок сохраняется с локальными датой и временем съёмки,
   подпись становится заметкой, AI при этом не вызывается: разбор и списание
   квоты происходят позже. Разбирать можно и в вебе (`/app/inbox`), и во
   вкладке «Инбокс» в Mini App — это один и тот же поток добавления еды,
   второго редактора черновика не появилось. Подробности — `docs/bot.md`.
2. **Напоминания.** Правила в `lib/reminders.ts`: не больше одного сообщения
   в день, тишина с 22:00 до 9:00, час дайджеста настраивается, писать
   только если есть о чём. Дайджест считает снимки за сегодня — иначе одно
   забытое фото превратило бы напоминание в ежедневный укор. Кнопка честно
   называется «Пауза на 3 дня», а не «напомнить позже»: до конца дня бот всё
   равно больше не пишет.
3. **Почтовая серия после калькулятора.** Три письма (сразу, +2 дня,
   +5 дней), тексты в `lib/email-series.ts`. Числа уходят из формы скрытыми
   полями — сервер не собирает рост, вес и возраст ради трёх писем, но и не
   верит числам на слово. Отписка: страница с кнопкой в тексте письма и
   отдельный POST-обработчик для `List-Unsubscribe` (RFC 8058).
   Подробности — `docs/email-series.md`.
4. **Планировщик** — `lib/scheduler.ts`, запускается из `instrumentation.ts`.
   Ни очереди, ни воркера: строка доставки со сроком, отметка о захвате и
   счётчик попыток уже дают всё нужное, а отдельный контейнер занял бы ещё
   сотню мегабайт.

Отдельно: `grammy` и `pg-boss` были установлены, а затем удалены. Боту нужно
пять методов API — фреймворк тянул бы за собой роутер апдейтов и middleware,
которые пришлось бы мокать в тестах; очереди у нас уже есть в виде таблицы.
Из зависимостей добавился только `nodemailer`.

## Что проверить руками при следующем заходе

- **SMTP у techperevod**: заданы ли реально `SMTP_HOST/USER/PASSWORD` в
  `.env.local` на их сервере. По git это не проверить (см. `shared-infra.md`).
  Без них письма пишутся в лог и никуда не уходят — это безопасное умолчание,
  а не поломка.
- **DNS домена**: SPF, DKIM и DMARC до включения рассылки. Иначе первые же
  письма уедут в спам, и репутацию домена придётся восстанавливать.
- **Вебхук бота** после деплоя: `node scripts/webhook.mjs set`, затем
  `node scripts/webhook.mjs info` — там должно быть пусто в ошибках доставки.

## Выкатка: что выяснилось на живом сервере

Сайт поднят 29 июля 2026. Пять вещей, которых не было видно из репозитория и
которые стоили вечера — все исправлены в коде и в инструкции:

1. **`techperevod.com` живёт на том же VPS.** В `shared-infra.md` значился
   отдельный сервер под FASTPANEL. Поэтому фронт — общий nginx, а не Caddy;
   конфиг в `deploy/nginx/`, `Caddyfile` остаётся для пустого сервера.
2. **Соседний Next.js занимает порт 3000.** Наш контейнер молча не
   поднимался, а nginx проксировал на соседа: главная отдавала 200 чужой
   страницей. Порт вынесен в `APP_HOST_PORT`, preflight предупреждает заранее.
3. **Дубликаты ключей в `.env`.** Побеждает последняя строка, а смотрят на
   первую — пустая строка из `.env.example` обнуляла заполненный токен.
   Preflight теперь называет такие ключи с номерами строк.
4. **`source .env` ломается** на значении вида `EMAIL_FROM=Живое Тело
   <hello@…>`: для shell это перенаправление ввода. В инструкциях значения
   читаются по одному ключу.
5. **Локальный кэш DNS на сервере — не признак** того, переехал ли домен:
   webroot-проверку делает Let's Encrypt со своей стороны. Смотреть надо
   внешний резолвер.

## Следующие шаги по приоритету

1. **Хвосты выкатки:** секреты автодеплоя, cron бэкапов, уведомление РКН —
   всё перечислено в «Состоянии» выше.
2. **Онбординг v2** — 10 шагов с живым пересчётом плана
   (`market-research.md`, раздел 2). Самый недооценённый рычаг конверсии.
   Заодно: форма подписки на письма есть только у расчёта энергии — она
   завязана на структуру `Targets`. Расчёт темпа и страницы блюд её ждут.
3. **Голос через бота** — GigaAM-v3 (открытая модель Сбера, MIT).
4. **Штрихкоды + Open Food Facts** с кэшем в отдельной схеме.
5. **Ещё блюда в `/skolko-kalorij`.** Раздел запущен на двенадцати; в плане
   тридцать самых частотных. Добавление — одна запись в `lib/dishes.ts`,
   страница и карта сайта подхватят её сами. Тесты проверяют данные, включая
   сходимость калорийности с БЖУ по Атуотеру.

## Почему работа «пропадала» и правило против этого

Контейнер, в котором идёт разработка, — одноразовый. Репозиторий в нём
клонируется заново при запуске, и клонируется **с ветки по умолчанию**,
то есть с `main`. Отсюда всё, что выглядело как откат:

- Незапушенный коммит после перезапуска контейнера не существует нигде.
  `node_modules` тоже: их надо ставить заново (`npm ci`).
- Даже запушенная работа выглядит пропавшей, если она лежит **только в
  рабочей ветке**: свежий клон встаёт на `main`, а там её нет.
- 30 июля так и было: `main` стоял на `273d735`, а рабочая ветка ушла
  вперёд на десять коммитов — весь Mini App v2, оптимизация расходов на AI
  и показ фото. Три раза за сессию рабочая копия «возвращалась» к состоянию
  двухдневной давности, и каждый раз это лечилось
  `git fetch origin <ветка> && git reset --hard origin/<ветка>`.
- Третье следствие, самое дорогое: автодеплой (`.github/workflows/deploy.yml`)
  запускается по пушу **в `main`**. Пока работа лежала в ветке, на сервер не
  уезжало ничего — выкатывать приходилось вручную.

**Правило: законченный кусок работы уходит в `main` в тот же заход.**
Коммит → пуш рабочей ветки → слияние в `main` → пуш `main`. Ветка после
этого остаётся на том же коммите, что и `main`; разница между ними — это
только незаконченная работа, и она не должна переживать день.

Проверить, что расхождения нет:

```
git fetch origin main
git rev-list --count origin/main...HEAD   # должно быть 0
```

## Рабочие мелочи

- Тестовый сервер поднимается скриптом `run-app.sh` в каталоге scratchpad;
  убивать процесс надо строго по имени `next-server`, иначе легко оставить
  старый инстанс со старым build-манифестом в памяти и получить 404 на
  чанках.
- Playwright не в зависимостях проекта: `tests/e2e/browser.mjs` ищет его там,
  где он установлен в среде. Тащить его в `npm ci` перед каждым деплоем ради
  ручных тестов не стоит.
- `tests/e2e/scheduler.mjs` запускается через `--import ./tests/e2e/alias-hook.mjs`:
  хук разрешает алиас `@/`, которым пользуются серверные модули. Скрипт
  очищает почтовые таблицы — только на тестовой базе.
- Антифлуд на AI — 3 секунды между разборами одного вида. В e2e, где два
  разбора идут подряд, приходится ставить паузу; это продуктовое правило, а
  не помеха тестам.

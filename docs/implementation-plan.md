# Живое Тело — план реализации

> Обновлено: июль 2026. Рабочий план разработки. Полная продуктовая
> спецификация — в [product-spec.md](./product-spec.md); этот документ
> определяет, **что и в каком порядке строим на самом деле**, с учётом малой
> команды. При противоречии приоритет у этого плана.
>
> Перед деплоем проведено исследование лидеров рынка, монетизации и
> open-source наработок — выводы и уточнённые приоритеты после M1:
> [market-research.md](./market-research.md).

## Зафиксированные решения

1. **Платформа запуска:** веб-SaaS на `jivoetelo.ru`. Telegram (бот / Mini App)
   — после работающего веб-ядра, на том же бэкенде.
2. **Хостинг:** VPS (reg.ru), Docker Compose, reverse proxy с TLS.
   Данные хранятся в РФ — это же закрывает требование локализации 152-ФЗ.
3. **База продуктов:** гибрид. LLM-оценки с диапазонами и пометкой
   «приблизительно» для обычных блюд; проверенные данные (штрихкоды — Open
   Food Facts, классические таблицы состава) для брендов; собственная
   верифицированная таблица растёт постепенно из пользовательских правок.
   Полноценную модерируемую базу из спеки (раздел 13.1) не строим заранее.
4. **Команда малая**, поэтому скоуп режется агрессивно (см. ниже).

## Отличия от спецификации (что сознательно отложено)

| Спека | Решение плана |
|---|---|
| Монорепо, NestJS, Redis, BullMQ, S3 (раздел 12) | Один Next.js-монолит + PostgreSQL + диск VPS. Разделяем на сервисы, только когда упрёмся. |
| Фаза 1: 7+ маркетинговых страниц, Storybook | Только текущий лендинг + рабочий лист ожидания. Интерактивное демо AI-камеры — позже, как отдельная задача. |
| Админ-панель (раздел 18) | Консоль БД / psql. Админку строим при реальной нагрузке на модерацию. |
| Pro для специалистов (фаза 4) | После появления удерживающихся пользователей. |
| Family, Pantry, планировщик, white-label | Вне горизонта планирования. |
| Telegram Mini App (фаза 5) | Прототип Mini App сделан раньше плана — переиспользует бэкенд веб-версии. Бот с напоминаниями — позже. |

Что из спеки **сохраняем как обязательное**: языковые правила и правила
безопасности (разделы 4.2–4.3), три уровня уверенности AI и уточняющие
вопросы (15.2–15.3), абстракция AI-провайдера (15.1), структурированный
вывод модели, диапазоны вместо ложной точности (14.1), режим «скрыть
калории».

## Архитектура

- **Приложение:** Next.js (App Router, `output: "standalone"`), TypeScript
  strict. Серверные действия / route handlers вместо отдельного API-сервиса.
- **БД:** PostgreSQL 17 + Drizzle ORM (миграции в `drizzle/`).
- **Файлы (фото еды):** диск VPS за тонкой абстракцией хранилища, чтобы позже
  переехать на S3-совместимое без переписывания кода.
- **AI:** интерфейсы `MealVisionProvider` / `NutritionAssistantProvider`
  (раздел 15.1 спеки) с первого дня; конкретный провайдер — деталь реализации
  за интерфейсом. Значения КБЖУ считает наш код, модель возвращает состав и
  диапазоны порций.
- **Очереди/фоновые задачи:** пока нет; AI-разбор — в рамках запроса с
  честным статусом обработки. Очередь появится вместе с реальной нагрузкой.
- **Деплой:** Docker Compose (`app` + `db`), reverse proxy на VPS —
  см. [deploy-vps.md](./deploy-vps.md).

## Майлстоуны

### M0 — фундамент (эта ветка) ✅

- Уход со стартера OpenAI Sites (vinext/Cloudflare) на самостоятельный Next.js.
- Рабочий лист ожидания: PostgreSQL + Drizzle, server action, валидация,
  защита от дублей.
- Docker-сборка и compose для VPS, документация деплоя.
- Этот план.

### M1 — в бою ✅

Сайт выкачен на VPS reg.ru 29 июля 2026: домен, TLS, миграции, контейнеры.

- ✅ `/api/health` + healthcheck контейнера, лимиты логов, `.dockerignore`.
- ✅ `deploy/Caddyfile` с TLS, заголовками безопасности и разрешением
  встраивания только для `/tg`.
- ✅ `deploy/migrate.sh` — идемпотентный раннер миграций (`schema_migrations`).
- ✅ `deploy/backup.sh` и `deploy/restore.sh` — база и фото, с ротацией.
- ✅ `npm run preflight` — проверка окружения до сборки.
- ✅ Шрифты вшиты в репозиторий: сборка больше не ходит на fonts.googleapis.com.
- ✅ `og.png` (2,2 МБ) → `og.jpg` 1200×630, 96 КБ.
- ✅ Выкатка на VPS: A-записи, сертификат Let's Encrypt, миграции, контейнеры.
  Фронтом оказался nginx, а не Caddy: на том же сервере уже жил
  `techperevod.com`. Порядок и грабли — в [deploy-vps.md](./deploy-vps.md),
  расклад по соседству — в [shared-infra.md](./shared-infra.md).
- ⏳ Вебхук бота, cron бэкапов, включение SMTP — остаток по deploy-vps.md.
- ⏳ Self-hosted аналитика посещений (Plausible/Umami).
- ⏳ Купить `zhivoetelo.ru` (стандартная транслитерация) с редиректом —
  блок в Caddyfile уже написан и закомментирован.

### M2 — ядро продукта: дневник с AI-разбором ✅

Одна вертикаль, без онбординга и целей:

- Аутентификация: e-mail + пароль или magic link (без SIWC, без соцсетей).
- Добавление приёма пищи текстом и фото → структурированный разбор
  (JSON-схема из раздела 15.2) → редактируемый черновик с уровнями
  уверенности и 1–2 уточнениями → сохранение.
- Лента дня и итоги (энергия, белок, клетчатка).
- Хранение фото с настройкой удаления.

**DoD:** новый пользователь регистрируется, логирует еду фото и текстом,
правит черновик, видит итоги дня. Языковые правила 4.3 соблюдены во всех
текстах интерфейса.

### M3 — цели и «что съесть дальше» v1 ✅

- Мини-онбординг (цель, профиль, активность, видимость калорий).
- Стартовые цели диапазоном (Миффлин-Сан Жеор, раздел 14.1).
- Вес + сглаженный тренд (EMA, раздел 14.3).
- Первая версия рекомендации «что съесть дальше»: детерминированные
  ограничения (остаток дня, исключения) + генеративное объяснение (15.5).

### M4 — удержание (частично ✅)

- ✅ Недельный обзор (8.12, редакционный формат, детерминированный текст).
- ✅ Адаптивная корректировка целей с подтверждением пользователя (14.2):
  шаг ±150 ккал, накопленный предел ±450, минимум 5 дней записей.
- ⏳ PWA-обвязка и e-mail-напоминания (без чувства вины, раздел 19) —
  перенесены ближе к деплою: напоминаниям нужна SMTP-инфраструктура.

### M5 — бесплатный доступ с защитой расхода ✅ (монетизация отложена)

Решение: пока всё бесплатно, монетизацию подключаем после набора подписчиков.

- ✅ Учёт расхода токенов (`ai_usage`), дневные лимиты на AI-операции,
  антифлуд и глобальный предохранитель по стоимости.
- ✅ Задел под тарифы: `users.plan`, `PLAN_LIMITS.premium` — код готов,
  никому не выдаётся. См. [free-tier.md](./free-tier.md).
- ✅ Интеграция Unitpay написана заранее и выключена: подпись, обработчик
  check/pay/error, идемпотентность. См. [payments.md](./payments.md).
- ✅ Экспорт и удаление данных — сделаны в рамках юридического блока (M7).
- ⏳ Включение оплаты и оферта на платные услуги — когда появятся подписчики.

### M6 — Telegram ✅ (кроме голоса)

- ✅ Mini App на `/tg`: «Сегодня», добавление еды текстом/фото с разбором,
  «что съесть дальше». Общий бэкенд с вебом, проверка подписи initData,
  привязка аккаунта одноразовым кодом, тема Telegram и MainButton.
  См. [telegram-mini-app.md](./telegram-mini-app.md).
- ✅ Бот на вебхуке с проверкой секрета: фото-инбокс, привязка кодом,
  вечерние напоминания без упрёка, пауза и `/stop`. Разбор снимков — в
  экране инбокса Mini App (строка с «Сегодня») и на `/app/inbox` в вебе,
  одним и тем же потоком. См. [bot.md](./bot.md).
- ⏳ Mini App v2 — оболочка на пять вкладок и мгновенный разбор в «Камере»
  сделаны; «Дневник», «План», «Профиль» — заглушки, ждут своей волны.
  См. [miniapp-v2.md](./miniapp-v2.md).
- ⏳ Голосовой ввод — следующий шаг (GigaAM-v3).

### M8 — почтовая серия ✅

- ✅ Форма подписки под калькулятором энергии с отдельным согласием и
  фиксацией редакции документов.
- ✅ Три письма с расписанием и сдвигом в дневное окно, отписка страницей и
  в один клик из почтового клиента. См. [email-series.md](./email-series.md).
- ✅ Планировщик в том же процессе: захват строки доставки, счётчик попыток,
  ограничение частоты. Отдельного воркера и очереди нет намеренно.
- ⏳ Настройка SPF/DKIM/DMARC и включение SMTP — на стороне сервера.

### M7 — юридический блок ✅ (в коде)

- ✅ Документы: соглашение, политика конфиденциальности, согласие,
  cookie — страницы `/legal/*`, реквизиты оператора из окружения.
- ✅ Два отдельных согласия при регистрации и согласие в листе ожидания,
  с фиксацией редакции документа в `user_consents`.
- ✅ Выгрузка всех данных (`/api/account/export`) и необратимое удаление
  аккаунта вместе с фотографиями.
- ✅ Дисклеймер «не медицинское изделие» на лендинге, в регистрации,
  в приложении и в Mini App.
- ⏳ Всё, что требует государства и юриста, — в [legal.md](./legal.md):
  регистрация ИП/ООО, уведомление РКН, уведомление о трансграничной
  передаче, проверка формулировок, товарный знак.

## Юридический чеклист

Полностью вынесен в [legal.md](./legal.md): что сделано в коде, что нужно
сделать вне кода и в каком порядке. Ключевое, что блокирует запуск:
регистрация оператора ПДн и уведомление о трансграничной передаче — до него
AI-разбор безопаснее держать выключенным (`AI_PROVIDER=off`).

## Главные риски

1. **Качество AI-разбора русских блюд** — валидировать на M2 на реальных
   тарелках, до инвестиций в M3+.
2. **Стоимость AI на пользователя** — под контролем: расход пишется в
   `ai_usage`, есть дневные лимиты и общий потолок. Считать реальную
   юнит-экономику по запросам из [free-tier.md](./free-tier.md).
3. **Доступность внешних AI API с VPS в РФ** — снят: используем готовый
   прокси-воркер `proxy.techperevod.com` на Hetzner из репозитория
   `techperevod`. См. [ai-proxy.md](./ai-proxy.md). Проверить на деплое.
4. **Один человек — узкое место** — не открывать новый майлстоун, пока не
   закрыт DoD предыдущего.

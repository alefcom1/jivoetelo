"use client";

/**
 * Заглушка вкладки «План». Экран (прогресс, динамика веса, приверженность,
 * адаптивная цель с объяснением — docs/miniapp-v2.md) строится отдельно,
 * параллельно с оболочкой; здесь только место в навигации, чтобы пять
 * вкладок Mini App v2 существовали уже сейчас. Заглушек с несколькими
 * экранами не рисуем намеренно — только текст, без имитации содержимого.
 */
export function PlanTab() {
  return <div className="tg-page">
    <header className="tg-hero">
      <p className="tg-kicker">План</p>
      <h1>Скоро</h1>
    </header>
    <p className="tg-hint">Прогресс, динамика веса и адаптивная цель появятся здесь.</p>
  </div>;
}
